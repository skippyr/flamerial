# Flamerial – LXTerminalk
## Install
### Procedures
- Replace the colors in the `general` section of `~/.config/lxterminal/lxterminal.conf` by the ones present in `flamerial.conf`.
- Restart LXTerminal.
